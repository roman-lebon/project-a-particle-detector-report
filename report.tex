\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx} 
\usepackage{listings} 
\usepackage{xcolor}
\usepackage{amsmath}
\usepackage{caption}
\usepackage[hidelinks]{hyperref}

\definecolor{codegray}{gray}{0.95}
\definecolor{commentgreen}{rgb}{0.0,0.5,0.0}
\definecolor{keywordblue}{rgb}{0.0,0.0,0.7}
\definecolor{stringred}{rgb}{0.6,0.0,0.0}

\lstdefinestyle{cpp}{
    language=C++,
    backgroundcolor=\color{codegray},
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{keywordblue},
    commentstyle=\color{commentgreen},
    stringstyle=\color{stringred},
    showstringspaces=false,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny,
    captionpos=b,
    tabsize=2
}
\lstset{style=cpp}

\title{A Particle Detector Simulation in C++}
\author{Roman le Bon \\ University of Manchester \\ PHYS30762 Object-Oriented Programming in C++}
\date{\today}

\begin{document}
\maketitle

% ABSTRACT  -- ~100-150 words. Write LAST.
% Rubric: states what was built, which OOP concepts were used, one result.
% Must NOT just restate the brief. Must show reasoning, not be purely descriptive.
\begin{abstract}
% TODO
\end{abstract}

\section{Introduction}

This project implements a simplified particle detector simulation in C++, inspired by the Compact Muon Solenoid (CMS) experiment at the Large Hadron Collider (LHS). CMS is a general-purpose detector at one of the LHC collision points, designed to identify stable particles produced in collisions and measure their momenta and energies so that the collision can be reconstructed for further analysis \cite{cmsdetector}. The aim of this project was not to reproduce the full complexity of CMS, but to build a smaller object-oriented model that captures the same basic idea: particles pass through several detector layers and produce different patterns of response. The physical model used in the program is based on a CMS-like detector made from sub-detector components. In the simulation, charged particles can be measured by a tracker, particles that deposit energy can be measured by a calorimeter, and muons can pass through to muon chambers. Neutral weakly interacting particles, such as neutrinos, are not directly detected in this simplified model and instead contribute to missing energy. The program therefore compares each particle's true energy with the energy measured by each detector component, then uses the pattern of measurements to infer the likely particle type.

The programming aim was to represent this physical situation using an object-oriented C++ design. The code is built around two main class hierarchies: one for particles and one for detector components. The particle hierarchy stores common physical quantities such as four-momentum, spin and rest mass, while derived classes represent specific particle types such as electrons, muons, taus, neutrinos, photons and hadrons. The detector hierarchy represents sub-detectors such as the tracker, calorimeter and muon chamber, each of which implements its own detection rule.

This structure was chosen because the problem naturally involves both shared properties, but different particle types need different additional data, such as lepton number, charge, lifetime or flavour. Similarly, all sub-detectors can attempt to measure a particle, but each sub-detector responds differently. Abstract base classes and virtual functions therefore allow the main detector code to treat particles and sub-detectors generically, while still calling the correct derived-class behaviour at run time.

The final program reads particles from an input text file, constructs a CMS-like detector from sub-detector components, passes each particle through the detector, prints the true and measured energies, infers the likely particle type from the detector signals, and calculates missing energy from invisible particles. Additional functionality includes a realistic calorimeter mode with Gaussian noise and exception handling for invalid particle input.

% 2. CODE DESIGN AND IMPLEMENTATION  -- ~50% of length (~1250 words)
% Rubric: emphasise advanced features; short, professionally-formatted
% code snippets; class diagram; reasoning behind EVERY advanced-feature
% decision (2 marks rely on this); reflection tied to actual classes.
% This is where the working notes get reshaped into argued prose.

\section{Code Design and Implementation}

\subsection{Class Hierarchies}
The design uses two main inheritance hierarchies, shown in Fig.~\ref{fig:uml}. The first hierarchy represents particles. The abstract \texttt{Particle} class stores the quantities shared by all particles, including four-momentum, spin, antiparticle status and rest mass. More specific particle classes then add only the data that they need: \texttt{Lepton} stores lepton number and charge, \texttt{Tau} adds a lifetime, \texttt{Neutrino} adds a flavour, and \texttt{Photon} and \texttt{Hadron} derive directly from \texttt{Particle}. This avoids repeating the same physical data in every particle class.
\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{Particle.png}
    \caption{UML class diagram showing the particle hierarchy, sub-detector
    hierarchy, and the relationship between \texttt{Particle} and}
    \label{fig:uml}
\end{figure}
% UML diagram:
% Export the final UML as a PDF or PNG and place it in the report repo.
% Suggested filename: uml_diagram.pdf or uml_diagram.png.

% \begin{figure}[h]
%     \centering
%     \includegraphics[width=\textwidth]{uml_diagram.pdf}
%     \caption{UML class diagram showing the particle hierarchy, sub-detector
%     hierarchy, and ownership relationship between Particle and FourMomentum.}
%     \label{fig:uml}
% \end{figure}

\subsection{Key Design Decisions}
% TODO - explain FourMomentum as a separate class.
% TODO - explain FourMomentum storage using std::array<double, 4>.
% TODO - explain why detector_response was removed from particles and detection
% logic belongs inside detector classes.
% TODO - explain perfect versus realistic calorimeter mode.
% TODO - explain inference logic from detector measurements.
% TODO - explain missing energy calculation and the max-reading fix.

\subsection{Advanced C++ Features}
% TODO - smart pointers:
% unique_ptr for owned objects such as sub-detectors and FourMomentum;
% shared_ptr for particles that are stored and processed by Detector.
% TODO - polymorphism through base-class references/pointers.
% TODO - file I/O using ParticleReader and particles.txt.
% TODO - exceptions using std::invalid_argument for invalid input.
% TODO - std::map for detector measurements.
% TODO - <random> for realistic calorimeter noise.
% TODO - use short code snippets only, not huge code dumps.

% 3. RESULTS  -- ~15% of length (~375 words)
% Rubric: illustrate how the code is used, including input AND output.

\section{Results}
% TODO - describe the input file particles.txt.
% TODO - include a short excerpt of particles.txt showing the format.
% TODO - show representative output from one or two particles.
% TODO - include detector measurements from Tracker, Calorimeter and MuonChamber.
% TODO - include one inference example.
% TODO - include missing energy result, e.g. 15 MeV if this is still the final
% output.
% TODO - explain briefly why the output validates the design.

% 4. DISCUSSION AND CONCLUSIONS  -- ~10% of length (~250 words)
% Rubric: concrete, implementable improvements. Do not just say "add more
% features". The challenge mark wants explicit discussion of extending YOUR
% OWN classes and design.

\section{Discussion and Conclusions}
% TODO - summarise whether the program met the aim.
% TODO - discuss limitations:
% no ECAL/HCAL split, so calorimeter response is simplified;
% tau decay products are not simulated;
% realistic calorimeter noise is simple Gaussian noise;
% inference logic is rule-based and could be generalised.
% TODO - discuss concrete extensions:
% split Calorimeter into ECAL and HCAL;
% add tau decay products;
% add more detailed detector resolution;
% replace detector-name checks in inference with a more general interface.
% TODO - explicitly mention how the class hierarchy makes these extensions
% possible.

% REFERENCES  -- not counted in word limit. Use one consistent style.

\begin{thebibliography}{9}

\bibitem{cmsdetector}
CMS Collaboration,
\textit{Detector},
CERN, CMS Experiment.
Available at: \url{https://cms.cern/detector}
[Accessed May 2026].

\end{thebibliography}

\end{document}
